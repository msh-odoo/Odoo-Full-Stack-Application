<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="quickbook_tag_action" model="ir.actions.act_window">
        <field name="name">Tag</field>
        <field name="res_model">quickbook.tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Search View -->
    <record id="quickbook_tag_search_view" model="ir.ui.view">
        <field name="name">quickbook.tag.search.view</field>
        <field name="model">quickbook.tag</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
            </search>
        </field>
    </record>

    <!-- List View -->
    <record id="quickbook_tag_list_view" model="ir.ui.view">
        <field name="name">quickbook.tag.list.view</field>
        <field name="model">quickbook.tag</field>
        <field name="arch" type="xml">
            <list string="Service Tags">
                <field name="name" />
                <field name="description" />
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="quickbook_tag_form_view" model="ir.ui.view">
        <field name="name">quickbook.tag.form.view</field>
        <field name="model">quickbook.tag</field>
        <field name="arch" type="xml">
            <form>
                <div class="oe_title">
                    <h1>
                        <field name="name" label="Name" />
                    </h1>
                </div>
                <group>
                    <field name="description" label="Description" />
                </group>
            </form>
        </field>
    </record>
</odoo>
